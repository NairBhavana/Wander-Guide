// src/index.js
import { registerRootComponent } from 'expo';
import App from './App'; // Ensure the App component is correctly imported

registerRootComponent(App);  // This is used in Expo instead of AppRegistry.registerComponent
